/**
 * Aqui entra a descrição da função.
 *
 * @param list - lista com número em formato de intervalo
 * @returns retorna se existe o número 5 na lista 
 */
 function tenhoNumero(list: string[]): boolean {
    return list.indexOf('5') >= 0;
}